# Core scientific Python
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11

# Hugging Face datasets (for NbBench PolyRx loading in notebooks 02-07)
datasets>=2.14
huggingface-hub>=0.17

# Antibody-specific
anarci>=1.3         # Only needed for notebook 01 (Harvey ingestion)
ablang>=0.3.1       # Only needed for notebook 01 (AbLang embedding)

# Plotting (for figure regeneration scripts)
matplotlib>=3.7
seaborn>=0.12

# Notebook execution (if not running in Colab)
jupyter>=1.0
ipykernel>=6.25

# Optional — recommended for progress bars and nicer table display
tqdm>=4.66
